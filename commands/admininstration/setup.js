const name = 'setup';
const type = 'admin';
const info = 'Setup server';

const run = (msg, client) => {
  
};

const setup = {
  name: name,
  type: type,
  info: info,
  run: run,
}

export { setup };